experiment_name: "mnist_hybrid_light"

data:
  dataset: "MNIST"
  encoding: "hybrid"
  num_qubits: 4
  num_features: 16
  num_classes: 2
  batch_size: 8
  num_train_samples: 128
  num_test_samples: 64

training:
  epochs: 3
  lr: 0.05
  optimizer: "Adam"
  checkpoint_dir: "checkpoints/lightweight/mnist_hybrid"
  checkpoint_prefix: "mnist_hybrid_light"
  save_start_epoch: 1
  save_interval: 1

environment:
  backend: "GPU"
  add_noise: false
